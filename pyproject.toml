[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "tp53-mutation-corrector"
version = "0.1.0"
requires-python = ">=3.9"
dependencies = [
    "biopython>=1.81",
    "requests>=2.31.0",
]
